{"type":"started","key":"v2:94f0a1747a57210cf8f0b0bca8bee2e27573c8ee8414a6df0ebc5a2ef7e6ad33","agentId":"af725009c9a360eec"}
{"type":"started","key":"v2:78131dff870cd15b920931dda55dfbc4f394578d6f17863dab52765b689832ac","agentId":"a0f3393004c2931e1"}
{"type":"started","key":"v2:451132186347e54f0d3847e195f9684bb27843c3da1b024e37a843c4edf772fe","agentId":"a0043525daec4e932"}
{"type":"started","key":"v2:83422586d469c58d76149ef8f73eb2429b27ddea47c5e0b727343ddcdd67b124","agentId":"a4816cadb510c99ba"}
{"type":"started","key":"v2:e88937874613ae99df0a80b54f7658628e0a100c4b838dc2360a1721c8c3014d","agentId":"ac42e3a830b175316"}
{"type":"result","key":"v2:451132186347e54f0d3847e195f9684bb27843c3da1b024e37a843c4edf772fe","agentId":"a0043525daec4e932","result":{"ideas":[{"title":"Glitch Hunter — an agent QA crew that playtests browser games and refuses to report any bug it cannot deterministically replay","user":"Solo indie HTML5/JS game developers (the itch.io / Ludum Dare crowd) who ship small games with zero QA staff and routinely break game rules when tweaking physics or scoring logic","bottleneck":"Manual playtesting is slow and non-exhaustive: rule regressions that only trigger after a specific move sequence (push two boxes into a corner, merge tiles in a certain order) are exactly what humans miss, and bug reports without repro steps are worthless. A naive 'play my game and find bugs' LLM prompt fails in a visible, demoable way: it hallucinates bugs that don't exist and misses the sequence-dependent ones.","agent_design":"Three-stage pipeline on the Claude Agent SDK, each stage earning its place: (1) SPEC LEARNER: an agent plays the known-good reference build through a headless Node harness (game exposes state() as JSON and act(move) — no vision needed, keeps cost low) and writes machine-checkable invariants ('boxes never occupy wall cells', 'score delta equals merged tile value') to a rules file — this is persistent MEMORY that makes bug detection a code check, not a vibe. (2) EXPLORER SWARM: an orchestrator dispatches 3 Haiku explorer subagents with distinct strategies (random fuzz, goal-directed solving, boundary probing) against the candidate build, logging state traces — ORCHESTRATION is load-bearing because a single trajectory can't cover the move-sequence space. (3) VERIFICATION GATE: any suspected violation must be reduced to a minimal action script and replayed deterministically twice before it may appear in the report; unreproducible suspicions are discarded. This gate is the star: it converts LLM 'noticing' into proof and is what separates the agent from the baseline. TOOLS: harness driver, trace differ, replay runner.","baseline":"One general-purpose Claude agent loop with the exact same harness tools (state/act/reset) and the same step budget, prompted once: 'Play this game and report any bugs with repro steps.' Judges run `python run_eval.py --system baseline` and `--system agent` on the same 15 builds.","primary_metric":"Verified-bug recall: fraction of 12 seeded rule mutations correctly detected WITH a working replay script, with false positives on 3 clean builds reported alongside (a report counts only if its replay actually reproduces the violation — scoring is mechanical, no LLM judge).","eval_plan":"15 synthetic eval cases built by mutation testing: take 2 tiny MIT-licensed (or self-written ~200-line) games — Sokoban and 2048 — and apply 12 one-line rule mutations (wall collision off-by-one, wrong merge score, undo duplicates a box, etc.), plus 3 unmutated clean builds to punish hallucinated bugs. Ground truth is the mutation itself, so scoring is a script. Deliberately hard case: a Sokoban mutation that only manifests after a specific 5-move corner sequence pushing two boxes — random play essentially never finds it, goal-directed exploration does. Baseline and agent run on identical builds with identical budgets; full result tables published.","feasibility":"Honest: doable in 48h with strict scope control — the games and harness are deliberately tiny (a day for harness + mutants, a day for agents + eval, half day for changelog/video). Biggest single risk: token cost and wall-clock of 15 cases x 2 systems x multi-agent exploration; mitigated by JSON-state (no screenshots), Haiku explorers, hard step caps, and invariant checks running in code rather than in the model. If the swarm is too slow, cut to 10 mutants and 1 game — the metric and story survive.","tech_stack":"TypeScript (or Python) Claude Agent SDK with Sonnet orchestrator + Haiku explorers; Node.js headless game harness exposing JSON state; plain-JS Sokoban/2048; a small browser renderer used only to record replay GIFs for the video; eval runner in Python printing the recall table; runs fine on Windows 11.","wow":"Split-screen video: baseline confidently files 3 bugs — two are hallucinated, and it walks straight past the corner-case mutation; agent side shows three explorers playing simultaneously, then a deterministic replay of a box clipping through a wall, auto-reduced to a 5-move repro script the developer can run. Hot take practically writes itself: 'LLMs are great at noticing weirdness and terrible at proving it — the replay gate deleted most of the baseline's bug reports, and that deletion IS the product.'"},{"title":"RedPen — a math-homework grader that pinpoints the first wrong line and backs every verdict with a computer-algebra counterexample","user":"High-school and intro-college math teachers and TAs (and tutoring platforms) who grade worked algebra/calculus solutions, not just final answers","bottleneck":"Grading the final answer is trivial; grading the WORK is the time sink — finding exactly where reasoning first breaks, for a stack of 30 papers. Pure-LLM graders fail in two embarrassing directions: they flag correct steps as wrong, and they give full marks to solutions where two errors cancel into the right final answer. A single-prompt grader visibly fails on that second case, which makes the improvement easy to demonstrate.","agent_design":"Pipeline where the LLM does language and a CAS does truth: (1) PARSER agent (Haiku) converts a numbered free-form solution into structured steps (expression + claimed operation). (2) VERIFIER is a SymPy TOOL, not a model: it checks consecutive-step equivalence (both-sides equality, solution-set preservation for hazardous ops like squaring or dividing by a variable) and, on failure, searches for a numeric substitution witness (plug x=2: LHS 7, RHS 9) — verification is load-bearing because it is the component the baseline fundamentally lacks. (3) DIAGNOSIS agent (Sonnet) runs only on SymPy-flagged lines and classifies the misconception against a misconception-playbook file — persistent MEMORY that grows across eval iterations and gives the changelog real evidence-linked entries ('added divide-by-possibly-zero pattern after case 14 failure: localization 80% -> 95%'). (4) Output gate: no line may be reported wrong without an attached counterexample, and the student-facing feedback must cite it. ORCHESTRATION is cheap-model-parses / tool-verifies / strong-model-explains, a purposeful cost-accuracy split.","baseline":"One direct Claude call: 'Here is the problem and the student's worked solution; identify the first incorrect line and explain the error.' Same model family, same cases, run via `python run_eval.py --system baseline`.","primary_metric":"First-error-line localization accuracy (exact line match against ground truth) across all cases; secondary: false-flag rate on fully-correct solutions. Scoring is exact string/line comparison — completely mechanical, no LLM judge.","eval_plan":"20+ cases synthesized in an afternoon with perfect ground truth: take 12 public problems (OpenStax / MATH dataset, both public), write correct worked solutions, then inject one known error at a known line (sign flip, bad distribution, illegal cancellation, dropped root) — the injection point IS the label; add 5 fully-correct solutions to measure false flags. Deliberately hard case: a solution containing two errors that cancel so the FINAL ANSWER IS CORRECT — the baseline passes it, ground truth says line 3 is wrong. Publish the full per-case table for baseline and final agent.","feasibility":"Highest-confidence build of any idea here: SymPy is mature, cases are hand-constructable in hours, prompts are small so API cost is trivial, everything runs locally on Windows. Biggest single risk: parsing messy free-form work into CAS-checkable steps — mitigated by constraining input to numbered plain-text/LaTeX lines (realistic for tutoring platforms) and treating unparseable steps as an explicit 'needs human' output rather than a guess. OCR/handwriting is explicitly out of scope.","tech_stack":"Python, Claude Agent SDK (Haiku parser + Sonnet explainer), SymPy for equivalence and counterexample search, a small pytest-style eval runner emitting a Markdown results table; optional tiny local web page for the live-demo input box in the video.","wow":"The 'right answer, wrong math' moment: baseline stamps a solution correct because the final answer checks out; RedPen flags line 3 with a concrete numeric counterexample and names the misconception — then the demo goes live and the judge types their own sabotaged solution and watches it get caught in seconds. Hot take: 'Near sign errors, model confidence is anti-correlated with algebraic truth — moving truth into SymPy and demoting the LLM to explainer took localization from roughly a coin flip to 95%.'"}]}}
{"type":"result","key":"v2:94f0a1747a57210cf8f0b0bca8bee2e27573c8ee8414a6df0ebc5a2ef7e6ad33","agentId":"af725009c9a360eec","result":{"ideas":[{"title":"RefGuard — a citation-forensics agent that catches fabricated and corrupted references in bibliographies, with registry-backed evidence for every verdict.","user":"Journal editors, PhD supervisors, and arXiv moderators triaging submissions in the LLM era — plus anyone reviewing an AI-assisted literature review — who must decide whether a bibliography can be trusted before wasting reviewer hours.","bottleneck":"Hallucinated citations are now endemic in AI-drafted papers, and checking a 25-entry bibliography by hand means 25 rounds of Google Scholar, Crossref, and publisher pages (30-60 min per paper). A single LLM pass is structurally unreliable: the model cannot know from its weights whether a paper exists, so it passes plausible fakes and flags obscure-but-real papers — exactly backwards from what an editor needs.","agent_design":"Claude Agent SDK (Python) orchestrator with four purposeful layers. (1) Parser agent turns raw bibliography text (BibTeX, numbered, or APA-style) into typed reference records, with a retry loop on malformed entries. (2) Parallel per-reference verifier workers (bounded concurrency ~5) with HTTP tools for Crossref, OpenAlex, and arXiv REST APIs (all keyless/public) plus rapidfuzz field-level matching; each worker emits a typed verdict {verified | minor-metadata-error | major-mismatch | likely-fabricated | unresolvable} with the registry record as evidence. Tools are load-bearing: existence is unknowable without lookup. (3) Shared memory: an on-disk SQLite cache of every query->response and resolved DOI — this gives dedup across refs sharing authors/venues, cuts cost/latency, and (shipped with the repo) makes judge reruns fully offline and deterministic. (4) Adjudicator agent that re-examines only low-confidence cases before any 'fabricated' verdict — it pulls the claimed first author's publication list from OpenAlex and searches title variants, because the naive rule 'not found => fake' produces false accusations on books, preprints, and non-DOI venues. Verification is the product: no verdict ships without an evidence trail (matched registry record or exhausted search log).","baseline":"One direct Claude call (same model, same verdict schema): the raw bibliography pasted into a single prompt asking it to label each entry verified/suspect/fabricated with reasons. Judges run both with `python run_baseline.py` / `python run_agent.py --offline` against the same eval files; a second cheap baseline (single agent with one generic web-search tool, no orchestration/adjudication) is included as an ablation.","primary_metric":"Per-reference verdict macro-F1 against ground-truth labels (with fabricated-detection F1 reported as the headline sub-metric), over ~240 labeled references.","eval_plan":"12 eval cases = 12 bibliographies (~20 refs each) harvested from real arXiv papers across CS/physics/bio. A corruption script (shipped, seeded) rewrites ~30% of entries with typed, labeled corruptions: swapped year, wrong venue, shuffled author list, plausible fabricated title attributed to real authors, and fully invented entries — ground truth is by construction. One deliberately hard case: a bibliography whose fakes are 'remix fabrications' (real authors, real venue, plausible title blending two genuine papers) mixed with real-but-obscure non-DOI refs (theses, workshop papers) that punish the plausible=real heuristic. Scoring is a deterministic script comparing predicted vs. gold verdict per entry; complete per-entry results published as CSV for baseline and final.","feasibility":"Honestly feasible: parser + workers + cache in ~1 day, adjudicator + eval harness + corruption generator in ~0.5 day, leaving 0.5 day for changelog, README, trajectories, and video. Crossref/OpenAlex/arXiv need no credentials. The single biggest risk is registry API flakiness or rate limits during judge reruns — mitigated by making the shipped response cache the default (--offline) mode so the main result reproduces with zero network, with --live as optional.","tech_stack":"Python 3.12, Claude Agent SDK (claude-agent-sdk), Anthropic API (Sonnet for workers, one Opus adjudicator call per suspect ref), httpx for Crossref/OpenAlex/arXiv REST, rapidfuzz for matching, SQLite for the query cache/memory, pytest for the eval harness, rich for terminal trajectory display.","wow":"The demo moment: baseline confidently certifies a beautifully fabricated citation and flags a real 1987 thesis as fake; RefGuard catches the fabrication and shows the OpenAlex evidence trail proving the real one exists — the exact inversion editors fear. Clean hot take for the rubric's insight row: 'LLMs judge citations by plausibility, verification systems must judge by provenance — and the expensive part isn't finding fakes, it's NOT accusing real obscure papers,' backed by the measured false-positive gap between baseline and the adjudicator ablation."},{"title":"DocDrift — a dataset-audit agent that verifies every claim in a data card against the actual data by writing, mutation-testing, and executing its own checks.","user":"Data engineers and ML practitioners inheriting public datasets (Kaggle, UCI, data.gov) or internal handoffs, who get burned when the README says 'no nulls in patient_id, dates cover 2015-2023, 12 categories' and the parquet file quietly disagrees.","bottleneck":"Data documentation drifts from data constantly, and today the check is either skipped or a half-hour of ad-hoc pandas per dataset. A single LLM prompt visibly cannot do it: a 500k-row file does not fit in context, so a one-shot answer over the doc plus head(50) is guessing — it misses violations that live in rare slices past row 500 and hallucinates confirmations of stats it never computed.","agent_design":"Claude Agent SDK (Python) pipeline where verification-of-the-verifier is the load-bearing trick. (1) Claim-extractor agent reads the data card and emits typed claims JSON (schema, row-count, range, null-rate, category-set, aggregate-stat, temporal-coverage), explicitly marking prose claims like provenance as 'unverifiable' — calibrated abstention is scored. (2) Check-synthesizer writes one pandas check function per claim. (3) Sandboxed executor tool runs checks against the FULL dataset on disk — data never enters model context, which is exactly why the agent beats the prompt. (4) Self-verification loop (the standout): before trusting any check, the agent generates a tiny mutated fixture that violates the claim and confirms the check fails on it; checks that pass mutants are rewritten — mutation-testing its own verifiers kills the 'green because the check is vacuous' failure mode. (5) Memory: a run ledger (JSON) of claim->check->verdict->computed evidence lets re-runs skip settled claims and doubles as the published trajectory; a lessons.md of check-writing pitfalls accumulated across eval iterations feeds the improvement changelog. (6) Reporter agent produces an audit a human would sign: per-claim verdict, computed value vs. claimed value, and offending row examples.","baseline":"One direct Claude call given the data card, column dtypes, and the first 50 rows, asked for the same per-claim verdict list — the honest version of 'just ask the model.' Same command interface: `python run_baseline.py case_07` vs `python run_agent.py case_07`; both fully offline, so judges reproduce bit-for-bit.","primary_metric":"Per-claim verdict F1 (claim-holds / claim-violated / unverifiable) scored against the ground-truth claim list, ~60-70 labeled claims across all cases; harness aligns agent output to gold claims by fuzzy text match, unmatched gold claims count as misses for both systems.","eval_plan":"12 cases from 6 public datasets (UCI Adult, NYC taxi sample, NOAA weather, e-commerce transactions, etc.): for each, I author an accurate data card, then a seeded corruption script injects 3-6 documented discrepancies (edit the card OR mutate the data) — ground truth by construction, labels emitted automatically. One deliberately hard case: a ~1M-row file whose only violation is a rare category appearing after row 800k plus a fuzzy-language claim ('roughly 10% missing' when truth is 14%) testing threshold judgment; head-of-file sampling provably fails it. Full per-claim result tables for baseline and final published in the repo.","feasibility":"Feasible with margin: extractor + executor + report in day 1, mutation-testing loop + eval harness day 1.5, evaluation runs are cheap and fast (offline, mostly Sonnet). Biggest risk is scoring alignment — matching the agent's extracted claims to gold claims — mitigated by scoring on the gold claim list with fuzzy matching so extraction misses are penalized identically for baseline and agent; fallback is lightly structuring the data cards as numbered statements, which is defensible and disclosed. No network needed at all, which de-risks the 15-pt reproducibility row completely.","tech_stack":"Python 3.12, Claude Agent SDK, Anthropic API (Sonnet default; Haiku for check synthesis as a costed ablation), pandas + pyarrow for execution, subprocess-sandboxed check runner with a resource cap, JSON run ledger, pytest eval harness, rich for live terminal trajectories.","wow":"Two-beat video: first the eval bar chart (baseline confidently 'verifies' stats it never computed; DocDrift catches the row-800k violation with the offending rows on screen), then the kicker — run it live on a REAL popular Kaggle dataset's actual data card and watch it find genuine documentation errors nobody planted. Hot take from an observed failure: 'the agent's first-draft checks passed vacuously ~20% of the time — LLM-written verifiers need mutation testing just like human-written ones,' with the changelog entry and before/after F1 to prove it."}]}}
{"type":"result","key":"v2:78131dff870cd15b920931dda55dfbc4f394578d6f17863dab52765b689832ac","agentId":"a0f3393004c2931e1","result":{"ideas":[{"title":"DeFlake — an agent that hunts, proves, and root-cause-fixes flaky tests, with a verification harness that catches 'fixes' that just hide the bug.","user":"A backend developer or CI maintainer on a Python team whose pipeline goes red 2-3 times a week from non-deterministic test failures, and whose current 'fix' is clicking re-run or slapping @retry on the test.","bottleneck":"A flake is invisible in any single run: reproducing it requires dozens of executions under randomized order, seeds, and timing, then a root-cause diagnosis (race, shared state, time boundary, test pollution). Humans shrug and re-run; naive LLM 'fixes' routinely make the test unconditionally green by weakening or deleting the assertion — which silently disables the bug detector. Neither a human in a hurry nor a one-shot prompt can do the statistics or the verification.","agent_design":"Claude Agent SDK orchestrator with a tight tool loop, where every capability is load-bearing: (1) TOOLS: a `run_suite(n, seed, order)` tool wrapping pytest + pytest-randomly/pytest-repeat that returns failure rates and per-run logs — the agent literally cannot diagnose a flake without executing many runs, which is exactly what the baseline lacks; (2) VERIFICATION (the core): a fix is accepted only if it passes a two-sided harness — 50 consecutive green runs under randomized order/seed AND the 'bug-twin' check, where the harness re-runs the fixed test against a mutated version of the code containing the real bug the test guards; if the test no longer catches the bug, the fix is rejected as assertion-weakening. This makes the degenerate fix (delete the assert, add sleep(5), pin the seed) mechanically impossible to pass; (3) MEMORY: after each solved case the agent appends a structured entry (symptom signature -> root cause -> remedy pattern) to a persistent flake playbook; later cases retrieve it first, and the eval reports iterations-to-fix per case to show the playbook measurably shortening runs (case 12 vs case 1); (4) ORCHESTRATION: a diagnose subagent (hypothesis + instrumentation) feeds a fix subagent, keeping the noisy 50-run logs out of the fixer's context — a deliberate context-budget choice to call out in the writeup.","baseline":"One direct Claude call (same model) given the test file, the relevant source file, and one failure log, asked to fix the flake — then scored by the exact same 50-run + bug-twin harness. Judges run `python run_eval.py --baseline` and `--agent` on identical cases. The baseline visibly fails in a demo-friendly way: it can't confirm the cause without re-running, and it frequently 'passes' the green check while failing the bug-twin check — caught cheating on camera.","primary_metric":"Verified-fix rate: fraction of the 12 eval cases where the patched suite passes 50/50 randomized-order runs AND the bug-twin mutant is still caught. (Secondary, for the changelog: iterations-to-fix per case, showing the memory playbook's effect.)","eval_plan":"12 fully synthetic mini Python projects (2-4 files each, suites under 2s), each seeded with one flaky test from a documented taxonomy: asyncio race, time.time() boundary, dict/set iteration order, shared tmp-file state, random seed dependence, float accumulation, threading, timezone, mocked-network jitter, resource leak across tests, module-level cache pollution, and the deliberately hard case — order-dependent pollution where the flaky test is the *victim*: it only fails when a different test runs first, so the fix must land in a file the failure log never mentions. Failure probabilities tuned to 30-60% so 50 runs are statistically decisive. Scoring is fully automatic (the harness emits pass/fail JSON per case); synthetic generation means judges reproduce byte-identical cases from a seed.","feasibility":"Honest yes for 48h solo: the 12 cases are small and a coding agent drafts them fast (day 0.5), the SDK loop + harness is day 0.5-1, eval + video is the last half day; total run cost roughly $10-20 of API and ~30-40 min wall-clock for the full eval (suites are 2s each, so 50 reruns x 12 cases is cheap). Single biggest risk: tuning the synthetic flakes so they actually flake reliably on the judges' machines (CPU-timing-based races can behave differently) — mitigated by preferring seed/order/state-based flakes over raw timing ones and by an injectable clock/jitter shim, verified in a clean venv before submission.","tech_stack":"Python 3.12, Claude Agent SDK (Python) with claude-fable-5 / a Sonnet-class model for cost, pytest + pytest-randomly + pytest-repeat, uv for clean-env reproduction, rich for the live 50-run verification board in the video, JSON-lines playbook file for memory, plain-Python mutation twins (no mutmut needed).","wow":"The video moment: the baseline 'fixes' a flake by deleting the assertion, the bug-twin check flashes red catching it in the act — then the agent's 50/50 green verification board fills live and case 10 resolves in 2 iterations because the playbook remembered case 3's pattern. Hot take earned from a real observed failure: 'An agent that can't re-run a test will make the flake invisible instead of fixing it — verification must reward still catching the bug, not just going green.'"},{"title":"DriftGuard — a docs-drift auditor that extracts every checkable claim in a project's docs, proves or refutes each one by executing it, and patches only what's actually wrong.","user":"An open-source maintainer or DevRel engineer for a CLI/library whose README and docs rot every release — users keep filing issues because a documented flag was renamed, a default changed, or the quickstart example throws on import.","bottleneck":"Docs drift is death by a thousand tiny lies: hundreds of individually checkable claims (flags, defaults, signatures, code examples, config keys) that no human re-verifies and no CI covers because they live in prose. A single-prompt LLM over docs+code is the worst of both worlds: it hallucinates drifts that aren't there (rewriting correct docs) and misses real ones, because it cannot execute anything and the full repo doesn't fit usefully in one context.","agent_design":"A three-stage pipeline on the Claude Agent SDK where orchestration and tools are the whole point: (1) EXTRACT: a claims-extractor subagent walks the docs and emits a typed claims ledger (claim text, kind: flag/default/signature/example/config, doc location, proposed oracle) — structured memory that decouples reading from checking and keeps each later step's context small; (2) VERIFY: per-kind verifier subagents resolve each claim against ground truth by execution, not reading — run `cli --help` and diff flags, import the module and inspect the live signature, execute the doc's code example in a sandboxed temp venv, parse config loading — stamping every claim CONFIRMED or DRIFTED with captured evidence (the exact command and observed output). Execution is the load-bearing capability the baseline structurally lacks; (3) FIX: a patcher edits only DRIFTED claims, then the verifiers re-run on the patched docs as the acceptance gate — a fix isn't done until its oracle passes. CONTEXT discipline is explicit and demo-able: each verifier sees one claim plus its evidence, never the whole repo, which is why it scales past the single-prompt baseline's context wall.","baseline":"One direct Claude call given the full docs plus concatenated relevant source, asked to list drifted claims and output doc patches — scored by the identical automatic scorer. Judges run both with `python run_eval.py --baseline|--agent`. The baseline's signature failure is confident false positives: it 'corrects' documentation that was already true, which the scorer counts against it and which makes a brutal on-camera contrast with the agent's evidence table.","primary_metric":"Drift-repair F1 against the injected ground truth: a true positive requires both identifying an injected drift AND producing a patch whose oracle passes on re-verification; any edit to a non-drifted claim counts as a false positive. One number, computed by a deterministic scoring script.","eval_plan":"10-12 small self-contained CLI/library projects (click or argparse based, a few hundred lines each, generated or adapted from public templates) each shipped with initially-accurate docs; an injection script then applies 3-5 drifts per project from a taxonomy — renamed flag, changed default, removed option, reordered/renamed function params, stale quickstart example, wrong env-var name, stale sample output — recording exact ground truth (~40 drifts total). Every project also carries planted traps: claims that look stale but are correct, to punish false positives. The deliberately hard case: the drift lives in a code example whose failure only appears at runtime (the API it calls was renamed, but the snippet still parses) AND sits next to a correct-but-odd-looking claim — pure reading gets it wrong in both directions; only execution separates them. Scoring is fully automatic against the injection manifest.","feasibility":"Realistic for 48h: everything is text plus subprocess, no fragile timing; sample projects and the injection script are a coding-agent-accelerated day, the pipeline is a day, eval/README/video the last half day; full eval costs roughly $10-25 in API and ~20-30 min wall-clock (tiny venvs cached after first create). Single biggest risk: scoring ambiguity when the agent's patch is correct but phrased differently than the canonical fix — mitigated by scoring via oracles (does the patched claim now verify?) rather than string-matching the patch, which keeps the metric objective.","tech_stack":"Python 3.12, Claude Agent SDK (Python), click/argparse sample projects, subprocess + isolated temp venvs (uv) for snippet execution on Windows, a JSON claims-ledger schema, deterministic scorer in plain Python, mkdocs-style markdown docs so patches are plain-text diffs.","wow":"The evidence ledger is the money shot: a table where every doc claim carries the literal command the agent ran and the observed truth — 'docs say --workers defaults to 4; ran cli --help; it is 8; DRIFTED; patched; re-verified' — right after the baseline is shown confidently rewriting three claims that were never wrong. Hot take from an observed failure mode: 'LLMs make docs more plausible, not more true — reviewing docs without executing them actively launders drift into confident prose, so every claim needs a runnable oracle.'"}]}}
{"type":"result","key":"v2:83422586d469c58d76149ef8f73eb2429b27ddea47c5e0b727343ddcdd67b124","agentId":"a4816cadb510c99ba","result":{"ideas":[{"title":"GroundedAppeal — a prior-authorization appeal-letter drafter whose adversarial fact-checker guarantees every clinical and policy claim is quote-backed by the patient chart, or the claim doesn't ship.","user":"Prior-authorization/appeals coordinators at clinics (medical-admin staff who draft insurance denial appeals all day). They are not clinicians and not lawyers, yet a single overclaimed fact ('patient completed 6 weeks of PT' when the chart shows 4) gets the appeal rejected and burns the patient's appeal window.","bottleneck":"Drafting one appeal means cross-reading three documents (denial letter, chart summary, payer policy criteria) and asserting only what the chart actually supports. It's slow (30-60 min/letter) and the failure mode is silent: a fluent letter that overstates evidence reads great and loses. A naive LLM makes this WORSE — it confidently claims criteria are met when the chart is silent or contradictory, which is exactly the costly convincing-but-wrong output this lens targets.","agent_design":"Claude Agent SDK pipeline where the checker, not the drafter, is the star: (1) Intake agent parses denial + chart + policy into a structured fact ledger with verbatim source spans — this ledger is the shared MEMORY all later agents read/write, so facts can't drift across revision rounds. (2) Coverage planner builds a criterion-by-criterion argument matrix (met / not met / unknown, each with evidence IDs) — ORCHESTRATION artifact that decides letter strategy before any prose exists. (3) Drafter writes the letter citing only ledger fact IDs. (4) Adversarial VERIFIER runs in a fresh context (never sees the drafter's reasoning, so it can't rubber-stamp): it extracts every factual assertion from the letter and must resolve each via a deterministic exact-span lookup TOOL over the source docs — a quote either exists or it doesn't, no vibes. (5) Revision loop (max 3 rounds): unsupported claims are removed or hedged; if a policy criterion is genuinely unmet, the agent concedes it and pivots to an exception pathway or tells the user what evidence to gather. Each capability earns its place because removing any one visibly reintroduces the baseline failure: no ledger → drift; no fresh-context verifier → self-grading; no span tool → hallucinated 'verification'.","baseline":"One direct Claude call: all three documents in context plus 'write a well-grounded appeal letter; only assert facts supported by the chart.' Same model, same documents, same output format. Judges run it with `python run_baseline.py --case N`.","primary_metric":"Fully-grounded letter rate: fraction of eval cases whose letter contains ZERO false or unsupported assertions, scored by (a) machine-checkable planted-trap assertions per case and (b) a claim-extraction + exact-span-match script applied identically to both arms. Target story: baseline ~2-4/12 clean, agent 10-12/12.","eval_plan":"12 fully synthetic cases (no real PHI, satisfies ground rules): a generator script + manual review produces denial letter, chart summary, payer policy, plus a ground-truth ledger and 3-5 planted traps per case (chart shows 4 weeks PT vs policy's 6; denial misquotes a dosage; a drug the chart says was never tried). Traps make most of the scoring deterministic — a script asks 'does the letter claim >=6 weeks of PT?' — deliberately limiting LLM-judge circularity. The one hard case: a denial that is actually CORRECT; the right output concedes the criterion is unmet and argues the exception pathway, while the baseline fabricates compliance. Full results table (per-case, per-trap) published for both arms.","feasibility":"Honestly feasible: documents are 1-2 pages each, the span-lookup tool is ~50 lines, and the case generator is a day-one task (generate with Claude, hand-audit each ledger — ~20 min/case). Budget: day 1 = generator + pipeline, day 2 morning = eval + changelog, afternoon = video. Biggest single risk: eval validity — if the claim-extraction step of the scorer is sloppy, the headline metric is contestable; mitigation is weighting the metric toward the deterministic planted traps and freezing the scorer before running either arm.","tech_stack":"Python 3.12, Claude Agent SDK (Python), claude-sonnet for all agents, plain JSON files for the fact ledger and argument matrix, a rapidfuzz/regex exact-span matcher, pytest-style eval harness, no external services. `pip install -r requirements.txt`, ANTHROPIC_API_KEY only; full eval run ~20 min, ~$4-6.","wow":"The hard case is the demo money-shot: given an appeal that SHOULD NOT be won, the baseline confidently fabricates that the patient meets the criteria; the agent's verifier strikes the claim on camera, and the final letter honestly concedes it and requests the missing PT records instead. Hot take earned from observed failures: 'the most valuable output of a drafting agent is the sentence it refuses to write' — verification as a product feature, not a QA step."},{"title":"TieOut — an earnings-summary agent where every number must reconcile to the source filing via a deterministic tie-out checker, or the summary doesn't ship.","user":"Buy-side analysts, financial journalists, and IR associates who turn earnings press releases into 250-word internal summaries within the hour after release. A summary with one wrong YoY percentage or basic-vs-diluted EPS mixup is worse than no summary — someone trades or publishes on it.","bottleneck":"Earnings releases bury figures in prose and tables; humans under time pressure and single-prompt LLMs both make the same errors: computing YoY off the wrong base year, quoting prior-year numbers as current, confusing GAAP with adjusted figures, and mangling millions vs billions. These errors are fluent and invisible — the definitional convincing-but-wrong financial summary.","agent_design":"Draft-check-revise loop where all arithmetic is forcibly delegated: (1) Parser agent extracts the filing's text and tables into a structured number store — figure ID, value, unit, period, GAAP/non-GAAP tag, source anchor — which persists as MEMORY across revision rounds. (2) Drafter writes the summary but may only cite figure IDs, never free-type numbers. (3) TIE-OUT CHECKER (the centerpiece): for every numeric claim in the draft it must either exact-match a store figure (unit-normalized) or recompute it with a calculator TOOL from two cited store figures (YoY %, margins) — deterministic pass/fail per claim, failures bounce back with the specific discrepancy. (4) Completeness checker verifies coverage (revenue, net income, both EPS lines, guidance if present, segments) so the agent can't 'win' by omitting numbers. (5) Final gate appends a lineage footnote table (every number → source anchor) — the artifact a real analyst would sign. The design thesis: the model never does math, it only picks operands; VERIFICATION is code, not another LLM opinion. Each piece is load-bearing: drop the calculator and YoY errors return; drop the store and unit confusion returns; drop completeness and the checker gets gamed by omission.","baseline":"One direct Claude call: full press-release text plus 'write a 250-word summary including the key figures and year-over-year changes.' Scored by the exact same frozen tie-out scorer. Judges run `python run_baseline.py` / `python run_agent.py` on the bundled filings.","primary_metric":"Clean-summary rate: fraction of eval cases whose summary contains zero incorrect or source-untraceable numeric claims, per the deterministic tie-out scorer (numeric-error rate per summary published as the secondary breakdown). Expected story: baseline ~3-5/12 clean, agent 11-12/12.","eval_plan":"12 real earnings press releases from SEC EDGAR (fully public, bundled as text in the repo with fetch script + URLs for reproducibility), spanning sectors and sizes. Scoring is deterministic and identical for both arms: a frozen extraction step pulls numeric claims from each summary, then code checks each against the source (unit/scale-normalized match within rounding tolerance, or recomputation from source figures). Ground truth IS the filing — no manual labeling needed for correctness, only a small hand-made coverage checklist per case (~15 min each). The one hard case: a filing with restated prior-year comparatives plus heavy non-GAAP adjustments, where the naive YoY computation is wrong even when both numbers are quoted correctly.","feasibility":"Feasible but the scorer IS the project: number normalization (\"$1.2 billion\" vs \"1,204\" in a thousands-denominated table, % vs pp, per-share rounding) is fiddly, so a full half-day is budgeted for the scorer alone, and it gets frozen and unit-tested before either arm runs — that freeze is also the fairness argument. Biggest single risk: messy EDGAR table parsing on some filings; mitigation is selecting the 12 cases partly for parseability and saying so honestly in the README. Day 1 = parser + loop + scorer; day 2 = eval, changelog, video.","tech_stack":"Python 3.12, Claude Agent SDK (Python), claude-sonnet, requests + beautifulsoup4 for EDGAR fetch/parse, pandas for the number store and results tables, decimal-based calculator tool, pytest for scorer unit tests. ANTHROPIC_API_KEY only; full 2-arm eval ~30 min, ~$5-8.","wow":"Live red-pen moment for the video: the scorer highlights the baseline's confident '+18% YoY' in red (real answer +11%, computed off the wrong base), then the agent's summary renders with every figure footnoted to its exact line in the filing — an output an analyst would genuinely sign. Hot take from an observed failure mode: 'LLMs don't need to get better at arithmetic — they need to be banned from it'; the fix wasn't a smarter model, it was removing the model's permission to type digits."}]}}
{"type":"result","key":"v2:e88937874613ae99df0a80b54f7658628e0a100c4b838dc2360a1721c8c3014d","agentId":"ac42e3a830b175316","result":{"ideas":[{"title":"TriMatch — an accounts-payable agent that three-way-matches vendor invoices against purchase orders and delivery receipts, and writes an exceptions report a controller would actually sign.","user":"An accounts-payable clerk / finance-ops analyst at a small company who must verify every vendor invoice against its purchase order and goods-received note before approving payment.","bottleneck":"Each invoice means opening three documents, aligning line items whose descriptions and orderings differ, recomputing unit-price extensions, tax, and freight, checking partial deliveries against what was already invoiced, and remembering every prior invoice to catch duplicates. It's 10-20 minutes per invoice, and every miss is direct cash loss (overbilling, tax skims, duplicate payment). Under volume, humans rubber-stamp.","agent_design":"Claude Agent SDK (Python) orchestrator running a per-case pipeline with four load-bearing capabilities. (1) Extraction subagents (one per document type, cheap model claude-haiku-4-5) emit strict JSON, each followed by a self-check pass that re-reads the source and diffs its own extraction — extraction errors poison everything downstream, so verification earns its place at the front. (2) A deterministic Python match-and-arithmetic tool the agent calls to align line items and recompute totals/tax/freight — LLMs are unreliable at arithmetic, so all numeric verification is code, not prose; the agent's job is interpretation, not addition. (3) Batch memory: a SQLite ledger of (vendor, invoice number, amount, date, line-item fingerprint) accumulated across the run — duplicate and near-duplicate detection is STRUCTURALLY impossible for a one-case-at-a-time baseline prompt, making memory the clearest agent-vs-baseline wedge. (4) An adjudicator agent (claude-opus-5) turns raw mismatches into human-readable exceptions with cited evidence, and a critic pass re-derives every proposed exception from the source documents before it's emitted — unconfirmed flags are dropped, which is what protects precision against hallucinated exceptions.","baseline":"One direct claude-opus-5 call per case with all three documents in context: 'List every discrepancy as JSON using this error taxonomy.' Same model, same documents, same scorer — a genuinely fair single-prompt baseline judges can run with one command.","primary_metric":"Exception-level F1 against seeded ground truth: a flag counts as correct only if it names the right error type AND the right invoice line/field. Precision and recall published per case.","eval_plan":"A generator script produces ~15 synthetic cases: clean PO/receipt/invoice triplets rendered as realistic documents, then 0-4 errors injected per case from a taxonomy (unit-price drift, quantity overbill, tax rate/rounding error, freight double-charge, wrong currency, billing undelivered items, duplicate invoice under a new number, partial-delivery over-invoicing) — ground truth is machine-readable by construction, so scoring is a deterministic script with zero judgment calls. Include 2-3 clean cases so false positives are punished. Hard case: one PO fulfilled by three partial deliveries across four invoices, where invoice #4 re-bills an already-invoiced line and a near-duplicate invoice arrives under a changed number — solvable only with batch memory plus cumulative arithmetic. Publish full per-case tables for baseline and final.","feasibility":"High for 48h solo: generator + scorer ~half a day, agent pipeline ~1 day, eval runs + changelog + video ~half a day. Biggest risk: PDF rendering/parsing flakiness eating hours — mitigate by generating simple text-styled PDFs (reportlab) with a structured-text fallback format, and by freezing the generator early so eval cases are stable while iterating on the agent.","tech_stack":"Python 3.12; Claude Agent SDK (Python) for orchestration/subagents; Anthropic API models claude-opus-5 (adjudicator/critic) and claude-haiku-4-5 (extraction subagents); reportlab to render synthetic PDFs, pdfplumber/pypdf to read them; SQLite for batch memory; a plain-Python deterministic scorer. Runs on Windows 11; approx cost per full eval run (15 cases, baseline + agent) on the order of a few dollars.","wow":"The video shows the same nasty case twice: the baseline confidently approves a duplicate invoice and misses a 0.5% tax skim; the agent's memory ledger catches the duplicate, the arithmetic tool catches the skim to the cent, and the final exceptions report reads like something a controller forwards to the vendor. Hot take earned from real iteration: 'the extractor lies confidently — the critic that re-derives every exception from source moved precision from ~0.6 to ~0.9, and deleting the fancy fuzzy-matcher experiment (changelog entry) actually improved F1.'"},{"title":"Playbook Redline — a contract-review agent that checks inbound vendor contracts against a company negotiation playbook and drafts evidence-cited redlines.","user":"A legal-ops manager or sole in-house counsel at a startup who reviews every inbound vendor MSA/DPA/NDA against the company's standard negotiation playbook before signature.","bottleneck":"Reading a 20-40 page contract against ~25 playbook rules takes hours, and the deviations that matter hide behind defined terms and cross-referenced carve-outs ('notwithstanding Section 9.2...'): the liability cap looks fine where you expect it and is quietly undone three sections later. Rushed reads miss exactly these, and consistency across reviews is poor.","agent_design":"Claude Agent SDK (Python or TypeScript) with a structure-first pipeline. (1) A structure pass builds a clause map and a defined-terms table — contracts are self-referential, and this context engineering is the point: without resolving 'Indemnified Parties' and section numbers, rule checks silently fail. (2) Per-rule checker subagents: each playbook rule is checked by a focused agent that receives ONLY the relevant clauses (retrieved via the clause map) plus the definitions it needs — deliberate context narrowing, because whole-contract prompts drown the signal (measurably: the baseline IS the whole-contract prompt). (3) A cross-reference resolver tool: when a clause cites another section, the checker must fetch and read it before ruling — this single tool is what catches the carve-out the naive read misses. (4) A verification gate: every finding must include an exact quote, and a verifier string-matches the quote against the contract text, rejecting unquotable findings — a mechanical hallucination killer. (5) A redline drafter (claude-opus-5) converts confirmed deviations into replacement language from the playbook's preferred positions and compiles the review memo. An optional batch-consistency memory (identical clause text gets identical verdicts) is run as an experiment and kept or removed based on evidence — a ready-made changelog entry either way.","baseline":"One direct claude-opus-5 call with the entire contract plus the full playbook: 'List every deviation with rule ID, section, and exact quote, as JSON.' Same model, same scorer, one command to run.","primary_metric":"Rule-level deviation-detection F1: ground truth is the set of (rule_id, section) deviations seeded at generation; quote accuracy (does the cited text actually appear in the contract) reported as a secondary integrity metric.","eval_plan":"Write the ~25-rule playbook once (liability cap >= 12 months' fees, no auto-renewal, governing law, payment terms <= 45 days, breach-notification window, no unilateral price increases, etc.). A generator assembles 12 synthetic contracts from a clause library based on open-source contract templates (Bonterms/Common Paper-style standard forms, rewritten), seeding each with 2-6 known deviations — some stated directly, some hidden behind a defined term or a cross-referenced carve-out. Only mechanically checkable deviations are seeded (numbers, named jurisdictions, clause presence/absence) so the deterministic (rule_id, section) scorer has zero ambiguity; hand-verify all 12 once. Hard case: liability cap facially compliant in Section 9 but voided by an 'uncapped for breaches of Section 6' carve-out sitting in the indemnity section, plus an auto-renewal buried in an order-form exhibit. Publish complete per-contract result tables for baseline and final.","feasibility":"Medium-high for 48h solo: the clause library + generator is the main lift (~half a day plus care), pipeline ~1 day, eval + video ~half a day. Biggest risk: ambiguous ground truth — a seeded 'deviation' a reasonable reader could dispute would poison the metric; mitigated by seeding only objective, mechanical deviations and keeping contracts as Markdown/plain text (no OCR risk at all, unlike PDF-first ideas).","tech_stack":"Python 3.12 (or TypeScript) with the Claude Agent SDK; claude-opus-5 for checkers/drafter, claude-haiku-4-5 for the structure/segmentation pass; contracts as Markdown; regex/string-match quote verifier and a deterministic (rule_id, section) scorer in plain Python — no external services, no OCR, fully offline-reproducible except API calls; a full eval run costs a few dollars.","wow":"The split-screen video moment: baseline prints 'Liability cap: compliant' — the agent's cross-reference resolver walks 9.2 to 14.3, flags 'cap is uncapped for data-breach claims via the Section 14.3 carve-out' with exact quoted text, and drafts the redline in playbook language. Hot take from a real observed failure: 'findings without an enforced verbatim quote were ~30% hallucinated; a dumb string-match gate beat every prompt-engineering attempt at honesty — verification you can grep is worth more than verification you can prompt.'"}]}}
{"type":"started","key":"v2:9074ab79fce9eae64bdc36626fcf3ae25952df7a394a552fcacf96b0a3f959e2","agentId":"ad8105c1bf2e4582f"}
{"type":"started","key":"v2:4a2073a2079e0d344fec8b52db320a274afbcb730a5f3f23bb38f66f079e6422","agentId":"aca5b74d5e975792d"}
{"type":"started","key":"v2:a463b5f84cb419ee30dae05238dcb9b80fcae20c9bcc6fb4c53f4cde0a0165fb","agentId":"a48dfb69cbf017acb"}
{"type":"result","key":"v2:a463b5f84cb419ee30dae05238dcb9b80fcae20c9bcc6fb4c53f4cde0a0165fb","agentId":"a48dfb69cbf017acb","result":{"rankings":[{"idea_number":1,"score":71,"rationale":"The bug-twin verification harness is genuinely clever engineering and the caught-cheating-on-camera moment is real, but the user is a developer fixing developer plumbing (navel-gazing penalty from my lens), and synthetic flakes misbehaving on judges' machines directly threatens the must-run-both reproducibility requirement."},{"idea_number":2,"score":75,"rationale":"The evidence ledger ('docs say 4, ran --help, it's 8') is legible to any judge and the false-positive trap punishing the baseline is a fair, sharp contrast; still a developer-tooling story with an OSS-maintainer user, and building 10-12 sample projects plus injection plus per-kind verifiers is more scope than it looks."},{"idea_number":3,"score":82,"rationale":"A vivid non-technical user (AP clerk), real cash stakes, and a demo (duplicate invoice + tax skim caught to the cent) any judge understands, with batch memory as a structurally honest agent-vs-baseline wedge; the risks are PDF extraction flakiness and an exception-level F1 scorer whose right-line/right-field matching can get contestable."},{"idea_number":4,"score":78,"rationale":"High-stakes user, and the cross-reference carve-out split-screen is one of the best before/after moments here, backed by a mechanical quote-verification gate; but the clause library + synthetic contract generator is the biggest single build lift on the list, and fully synthetic contracts slightly dent the 'real person would sign this' credibility."},{"idea_number":5,"score":87,"rationale":"Timely problem every judge has personally seen (hallucinated citations), real arXiv data, a perfectly legible inversion demo (certifies the fake, accuses the real thesis), and the shipped offline cache nails the reproducibility row cold; main weakness is that the corruption-script eval leans synthetic and per-reference verdict work is somewhat pipeline-ish rather than deeply agentic."},{"idea_number":6,"score":81,"rationale":"Mutation-testing its own checks is the most original verification idea in the set, fully-offline reproduction de-risks 15 points, and the live run on a real Kaggle data card finding unplanted errors is a great kicker; weakness is a somewhat developer-ish user and a claim-alignment scoring step that could get mushy."},{"idea_number":7,"score":79,"rationale":"The most emotionally resonant user and stakes on the list, and the concede-the-losing-case hard case is a superb end-to-end-quality and hot-take moment; but the headline metric depends on a claim-extraction scorer the author admits is the weak point, and synthetic medical cases plus partial LLM-judge circularity make the measured-improvement row contestable."},{"idea_number":8,"score":80,"rationale":"Real SEC filings give it the most credible data of any idea, the red-pen wrong-YoY demo is instantly legible, and 'ban the model from arithmetic' is a clean thesis; the risk is that the tie-out scorer IS the project — unit normalization and EDGAR parsing can eat the 48 hours and a contested scorer undermines the headline number."},{"idea_number":9,"score":66,"rationale":"The replay-gate insight is strong and box-clipping GIFs make a fun video, but the scope (harness + two games + 12 mutants + a three-strategy swarm + spec-learning invariants, solo in 48h) is the least believable here, so the finished submission likely ships thin, slow, or cut down, hurting end-to-end polish and the measured story."},{"idea_number":10,"score":76,"rationale":"Universally understandable problem, the two-errors-cancel demo plus live judge-typed sabotage is terrific video, and it's the safest build with mechanical scoring; but the agentics are thin (parse, call SymPy, explain), which caps the 30-point engineering row where purposeful orchestration, memory, and context work carry most weight."}]}}
{"type":"result","key":"v2:4a2073a2079e0d344fec8b52db320a274afbcb730a5f3f23bb38f66f079e6422","agentId":"aca5b74d5e975792d","result":{"rankings":[{"idea_number":1,"score":81,"rationale":"Strongest agent-engineering story in the set: the bug-twin check makes assertion-weakening mechanically impossible, scoring is fully automatic, and memory has a measurable effect (iterations-to-fix). Main weakness is scope weight plus the risk that synthetic flakes behave differently on judges' machines, though the seed/order-based taxonomy mitigates most of it."},{"idea_number":2,"score":74,"rationale":"Execution-backed oracles and re-verification scoring make the metric objective, and the false-positive traps give a brutal, fair baseline contrast. Weakness: four different per-kind verifiers (help-diff, import-inspect, venv snippet execution, config parsing) are each fiddly plumbing, so the 48h budget is tighter than the writeup admits."},{"idea_number":3,"score":75,"rationale":"Highest feasibility confidence among the document ideas — ground truth by construction, deterministic arithmetic scorer, and batch memory is a genuinely structural wedge over the per-case baseline. Weakness: the pipeline is conventional extract-verify-adjudicate, and the PDF layer adds risk for little rubric payoff (the text fallback is the honest path)."},{"idea_number":4,"score":70,"rationale":"The cross-reference resolver and greppable quote gate are purposeful, and Markdown-only contracts kill OCR risk entirely. Weakness: the clause library plus generator with mechanically checkable seeded deviations is the real lift and is underbudgeted at half a day, and LLM contract review is familiar territory judges will grade skeptically."},{"idea_number":5,"score":73,"rationale":"The provenance-vs-plausibility wedge is airtight, and shipping the SQLite cache as a default offline mode is exactly the right reproducibility move for a network-dependent idea. Weakness: gold labels for the ~70% uncorrupted real-world entries aren't truly by construction (obscure-but-real refs can be legitimately unresolvable), which softens the headline F1's cleanliness."},{"idea_number":6,"score":78,"rationale":"Mutation-testing the agent's own checks is the most novel verification idea here, the whole eval is offline and deterministic, and the live run on a real Kaggle data card is a strong closing beat. Weakness: claim-to-gold fuzzy alignment is the scoring soft spot, and public-dataset fetching plus a 1M-row file adds mild clean-clone friction."},{"idea_number":7,"score":72,"rationale":"The fresh-context verifier with exact-span lookup and the concede-the-losing-case hard case directly hit the end-to-end-quality and hot-take rows, on a problem with real stakes. Weakness: a binary per-letter metric over only 12 cases is coarse, and the claim-extraction half of the scorer is the contestable part the whole headline number leans on."},{"idea_number":8,"score":66,"rationale":"'Ban the model from arithmetic' is a crisp thesis with real filings and a bundled-text reproduction path. Weakness: the deterministic scorer must extract and normalize numeric claims from free prose against messy EDGAR tables — the project's own writeup admits the scorer IS the project, which is a double fiddliness risk (parser and scorer) a solo 48h build can easily sink into."},{"idea_number":9,"score":63,"rationale":"The replay-or-it-didn't-happen gate is a great concept with mechanical mutation-based scoring and the most visual demo of the set. Weakness: the headline metric depends on Haiku explorers actually discovering sequence-dependent bugs — an uncontrollable outcome — so recall may underwhelm, and two games plus harness plus swarm plus spec learner is the most overloaded 48h scope here."},{"idea_number":10,"score":71,"rationale":"Bulletproof build: mature SymPy, hand-constructable cases with exact-line ground truth, trivial cost, and the errors-that-cancel demo lands the improvement story cleanly. Weakness: it is the thinnest agent system of the ten — essentially parse, CAS-check, explain — so the 30-point Agent Solution row (and the tie-breaker) caps it despite near-perfect reproducibility."}]}}
{"type":"result","key":"v2:9074ab79fce9eae64bdc36626fcf3ae25952df7a394a552fcacf96b0a3f959e2","agentId":"ad8105c1bf2e4582f","result":{"rankings":[{"idea_number":1,"score":86,"rationale":"The bug-twin mutant check is a mechanically un-gameable verification gate that makes the baseline fail on camera for a provable reason, and every agent component (multi-run tool, memory playbook with measured iterations-to-fix, context-split subagents) is load-bearing with its own evidence line. Main weakness: synthetic flakes must actually flake at the tuned 30-60% rate on judges' machines — the seed/order/state mitigation is right, but one non-reproducing case on a clean Windows env dents the reproducibility row."},{"idea_number":2,"score":79,"rationale":"Oracle-based scoring (does the patched claim now verify by execution) sidesteps the patch-phrasing ambiguity that kills most doc projects, and the planted correct-but-odd-looking traps make the false-positive story quantitative. Weakness: building 10-12 projects plus per-kind verifiers (flags, signatures, examples, config) is a wide build surface for 48h, and the verification trick is solid engineering rather than novel — expect a strong but not standout Agent Engineering row."},{"idea_number":3,"score":74,"rationale":"Deterministic seeded-error scoring, clean cases punishing false positives, and code-not-prose arithmetic are exactly right, and the exceptions report is a signable artifact. Weakness: the headline duplicate-detection win is partly baseline-hobbling — a per-case prompt structurally cannot see the batch, so the wedge is designed in rather than earned — and PDF render/parse fiddliness is a known time sink."},{"idea_number":4,"score":70,"rationale":"The string-match quote gate and mechanically-checkable-only seeded deviations keep the metric objective, and the cross-reference carve-out hard case is a genuine agent-vs-prompt separator. Weakness: authoring a credible clause library plus 12 contracts with unambiguous ground truth is the biggest human-judgment lift of any idea here, and if the seeded deviations are mechanical enough to score deterministically, a skeptic will ask how much of the task truly needed agents."},{"idea_number":5,"score":77,"rationale":"Tools are provably load-bearing (existence is unknowable from weights), the shipped offline cache makes judge reruns deterministic, and the adjudicator ablation quantifying the false-accusation gap is a ready-made measured-improvement story. Weakness: the 5-class verdict scheme invites gold-label ambiguity at the minor-error/major-mismatch and real-but-unresolvable boundaries, and citation checking is a well-trodden genre, capping the engineering-novelty ceiling."},{"idea_number":6,"score":82,"rationale":"Mutation-testing the agent's own generated checks kills the vacuous-green failure mode with a measurable before/after, and the fully-offline design plus full-dataset execution makes both the baseline gap structural and the reproducibility row nearly free. Weakness: the primary metric leans on fuzzy claim-to-gold alignment — the numbered-statement fallback fixes it but makes the data cards feel lab-conditioned, a hook a skeptical judge will pull on."},{"idea_number":7,"score":63,"rationale":"The strongest user-value narrative and the correct-denial hard case are excellent, and the fresh-context span-match verifier is a real hallucination killer. Fatal-ish weakness for this judge: the primary metric (zero unsupported assertions) is maximized by a letter that asserts almost nothing — no completeness term is in the metric — and 12 binary case outcomes plus an LLM-ish claim-extraction scorer make the headline number both coarse and contestable."},{"idea_number":8,"score":71,"rationale":"Real EDGAR filings, a frozen unit-tested scorer, and the completeness checker that blocks winning-by-omission show unusually honest eval design, and the 'ban the model from arithmetic' thesis is crisp. Weakness: the scorer genuinely is the project — number extraction/normalization from free-text summaries and messy EDGAR tables can easily eat a full day, and if the scorer's own claim-extraction step wobbles, the clean-summary rate (only 12 binary data points) becomes contestable."},{"idea_number":9,"score":67,"rationale":"Mutation-seeded ground truth plus the deterministic replay gate gives fully mechanical scoring, and giving the baseline identical tools and budget is the fairest baseline in the set. Weakness: scope — two game harnesses, 12 mutants, a three-strategy swarm, and automatic repro minimization is the riskiest 48h build here, and detection hinges on the spec-learner deriving the right invariants, so the finished recall number could land unimpressively low even after the fallback cuts."},{"idea_number":10,"score":73,"rationale":"The most bulletproof eval of the ten — exact-line mechanical scoring, 20+ cheap cases, and the cancelling-errors hard case that provably breaks any answer-checking baseline — with near-zero delivery risk. Weakness: it is light on the 30-point agent-engineering row — a parser, one CAS tool, and an explainer is closer to a smart script than an orchestrated system, and judges will note SymPy, not the agent, is doing the load-bearing work."}]}}
{"type":"started","key":"v2:dc2a6fb08934a270bd953e906ef0eb73c41697338085fe01ea02bd8fa44bb87c","agentId":"ab34dc52c1d376d51"}
{"type":"result","key":"v2:dc2a6fb08934a270bd953e906ef0eb73c41697338085fe01ea02bd8fa44bb87c","agentId":"ab34dc52c1d376d51","result":"# DocDrift — 44-Hour Build Plan\n\nA dataset-audit agent that verifies every claim in a data card against the actual data by writing, mutation-testing, and executing its own checks.\n\n---\n\n## 1. Pitch\n\nData engineers and ML practitioners who inherit datasets — a Kaggle download, a UCI classic, an internal handoff — routinely trust a README that says \"no nulls in `patient_id`, dates cover 2015–2023, 12 categories\" while the parquet file quietly disagrees; today the check is either skipped or a half-hour of ad-hoc pandas per dataset. A single LLM prompt visibly cannot do this job: a 500k-row file does not fit in context, so a one-shot answer over the doc plus `head(50)` is guessing — it misses violations hiding in rare slices past row 500 and hallucinates confirmations of statistics it never computed. DocDrift is a Claude Agent SDK pipeline that extracts typed claims from the data card, synthesizes one pandas check per claim, **mutation-tests each check against a deliberately-violating fixture before trusting it** (killing the \"green because the check is vacuous\" failure mode), executes checks against the full file on disk (data never enters model context — exactly why the agent beats the prompt structurally), and emits an audit report a human would sign, with computed-vs-claimed values and offending rows. The whole evaluation is offline and deterministic, so judges reproduce it bit-for-bit.\n\n---\n\n## 2. Repo Layout\n\n```\ndocdrift/\n├── README.md                     # pitch, architecture diagram, results table, hot take\n├── CHANGELOG.md                  # Improvement Changelog: one entry per iteration + evidence links\n├── REPRODUCE.md                  # clean-env guide: exact commands, versions, runtime, cost\n├── DISCLOSURE.md                 # coding-agent usage disclosure + pointers to trajectories/\n├── pyproject.toml                # pinned deps (uv-compatible)\n├── .env.example                  # ANTHROPIC_API_KEY=\n├── Makefile                      # make data / make eval / make demo (+ tasks.ps1 for Windows)\n├── run_agent.py                  # python run_agent.py case_07  → runs/case_07/agent/\n├── run_baseline.py               # python run_baseline.py case_07 → runs/case_07/baseline/\n├── src/docdrift/\n│   ├── orchestrator.py           # asyncio pipeline; fan-out over claims, Semaphore(4)\n│   ├── schemas.py                # pydantic models: Claim, Check, MutantResult, Verdict, Ledger\n│   ├── agents/\n│   │   ├── extractor.py          # data card → typed claims JSON (with quoted spans)\n│   │   ├── synthesizer.py        # claim → pandas check function (source string)\n│   │   └── reporter.py           # ledger → signed-quality markdown audit\n│   ├── tools/\n│   │   ├── profile.py            # dtypes + head(20) snapshot (the ONLY data in context)\n│   │   ├── executor.py           # sandboxed subprocess check runner (timeout, temp cwd)\n│   │   └── mutants.py            # builds tiny violating fixture per claim type\n│   ├── ledger.py                 # run ledger read/write; skip-settled-claims on re-run\n│   └── lessons.py                # loads lessons.md into synthesizer system prompt\n├── lessons.md                    # accumulated check-writing pitfalls (memory across cases)\n├── eval/\n│   ├── make_cases.py             # builds cases/ from data_src/ + corruption specs (seeded)\n│   ├── corruptions.py            # the 8 corruption operators (edit card OR mutate data)\n│   ├── specs/case_01.yaml … case_12.yaml   # which dataset, which corruptions, seed\n│   ├── score.py                  # span-anchored alignment + macro-F1; emits results.md/json\n│   ├── run_all.py                # baseline + agent over all cases; writes results/\n│   └── gold/case_XX_gold.json    # generated ground truth (committed for judge convenience)\n├── data_src/                     # committed small raw datasets + SHA256SUMS (licenses noted)\n│   ├── adult.csv  penguins.csv  winequality-red.csv  online_retail_sample.csv\n│   └── taxi_jan2023_50k.parquet  noaa_gsod_sample.csv\n├── cases/                        # GENERATED by make data: case_XX/{data.parquet, datacard.md}\n├── results/                      # full per-claim tables for baseline and final (committed)\n├── runs/                         # per-run ledgers, check source, executor logs\n├── trajectories/                 # committed representative agent trajectories (all agents)\n│   ├── agent_case_04_full.jsonl  agent_case_12_hard.jsonl\n│   ├── baseline_case_04.json\n│   └── coding_agent/             # Claude Code session excerpts used to build the repo\n└── video/script.md\n```\n\n---\n\n## 3. Agent Architecture\n\n**Orchestration mechanism:** plain Python **asyncio pipeline** in `orchestrator.py`; each stage is a fresh `ClaudeSDKClient` session (Claude Agent SDK, Python) with stage-specific system prompt and **in-process MCP custom tools** (`@tool` + `create_sdk_mcp_server`). Per-claim work (synthesize → mutation-test → execute) fans out with `asyncio.Semaphore(4)`. No filesystem/bash tools are granted to the model — only the three custom tools below — so the trajectory is small, auditable, and every action is purposeful. Deliberate choice to call out in the README: the orchestrator is deterministic Python and only *judgment* steps are LLM calls.\n\n| Stage | Exact job | Capability used | Why it earns rubric points |\n|---|---|---|---|\n| **1. Claim Extractor** (Sonnet) | Reads `datacard.md`, emits typed claims JSON: `{id, type ∈ {schema, row-count, range, null-rate, category-set, aggregate-stat, temporal-coverage, prose-unverifiable}, quoted_span, params}`. Must quote the exact card substring per claim. Prose claims (provenance, collection method) are marked `unverifiable` — calibrated abstention is scored. | **Context engineering** — sees only the card + a `profile.py` snapshot (dtypes, head(20)); never raw data. | Shows purposeful context: extraction quality is isolated from data size, and quoted spans make scoring deterministic (see §5). |\n| **2. Check Synthesizer** (Sonnet; Haiku as costed ablation) | Per claim, writes one self-contained `def check(df) -> {passed, computed, evidence_rows}` pandas function as a source string. System prompt includes current `lessons.md`. | **Tools + memory** — output is executable code, not an opinion; lessons.md injects accumulated pitfalls (dtype coercion, NaN semantics, tz-naive dates). | Code-as-verdict is the structural beat over the baseline; the memory file measurably reduces retries across cases (reported as retries-per-claim, case 1 vs case 12). |\n| **3. Mutation Verifier** (Python + Sonnet on failure) | Before any check is trusted: `mutants.py` builds a ~20-row fixture that *satisfies* the claim and a mutated twin that *violates* it (null injected, category added, range breached, count off-by-one…). The check must pass the clean fixture and **fail the mutant**. A check that passes its mutant is vacuous → sent back to the synthesizer with the mutant diff; two strikes → claim marked `unverifiable(check-failed)` rather than trusted. | **Verification (the load-bearing trick)** — mutation-testing the agent's own verifiers. | This is verification *of the verifier*, with a before/after F1 in the changelog — the most direct answer to \"purposeful verification\" in the rubric. |\n| **4. Sandboxed Executor** (custom tool, no LLM) | Runs the validated check against the **full** dataset in a `subprocess` (fresh interpreter, temp cwd, 60s timeout, output capped at 4KB, no network required by design). Returns `{passed, computed, evidence_rows(≤5)}` to the orchestrator; only the *summary* re-enters model context. | **Tools + context discipline** — the full data is touched exactly once, on disk, outside context. | Makes the baseline gap structural, not prompt-luck: the agent computes what the baseline can only guess. |\n| **5. Run Ledger** | Append-only JSON per run: claim → check source → mutant result → verdict → computed evidence → timing/cost. Re-runs skip settled claims (hash of claim + data fingerprint). Doubles as the published trajectory. | **Memory + reproducibility** | One artifact serves three deliverables: trajectory, evidence trail, re-run cache. |\n| **6. Reporter** (Sonnet) | Ledger → `audit.md`: per-claim verdict table (claimed vs computed), offending row examples, abstentions with reasons, one-paragraph executive summary. | **End-to-end quality** | The output a data engineer would actually paste into a handoff thread — targets the 20-pt \"would sign their name to it\" row. |\n\n---\n\n## 4. Baseline\n\n`run_baseline.py case_XX`: **one direct Claude call** (same model, Sonnet) given the full data card, the column dtypes, and the first 50 rows as CSV text, asked for the identical per-claim output format (quoted span + verdict ∈ {holds, violated, unverifiable}). Same CLI shape, same output schema, same scorer.\n\nWhy judges will consider it fair:\n\n- It is the *honest* version of \"just paste it into Claude\" — what a practitioner actually does today, with the same model and the same information a human skimmer uses.\n- **Baseline-plus preempts the strawman objection:** a second variant additionally receives `df.describe(include='all')` and per-column `value_counts().head(10)` — the best single-prompt version that still fits context. Both baselines are scored and published; the agent must beat both.\n- Identical claim inventory treatment: both systems extract their own claims, both are scored against the same gold list by the same alignment rule, and an unmatched gold claim counts as a miss for either system.\n- Both run offline against local files with pinned model + `temperature=0`, so judges reproduce both sides with one command each.\n\n---\n\n## 5. Eval Harness\n\n**Primary metric:** macro-F1 over the three verdict classes {claim-holds, claim-violated, unverifiable}, computed per gold claim across all cases (~70 gold claims over 12 cases). Secondary: violation recall (the metric users feel), false-alarm rate, cost and wall-clock per case.\n\n**Alignment (defusing the judges' main worry):** gold claims are *defined as character spans* in each data card (the corruption generator knows exactly which sentence it wrote or perturbed). Both systems must output the exact quoted substring for every claim; alignment is **span-overlap ≥ 0.5 IoU** — deterministic, auditable, no mushy fuzzy matching in the primary path. `rapidfuzz` token-set ratio ≥ 85 is a *fallback only* (used when a system paraphrases), and every fallback match is flagged in the published results so a skeptical judge can inspect all of them. Cards stay natural README prose — no numbered-statement lab conditioning.\n\n**Cases (12, from 6 datasets, ground truth by construction):**\n\n- **Committed directly in `data_src/`** (small, permissive licenses, no download): UCI Adult (census income, CC BY 4.0, ~48k rows), Palmer Penguins (CC0), UCI Wine Quality (red), UCI Online Retail II 40k-row sample, NYC Yellow Taxi Jan-2023 50k-row parquet sample (public domain), NOAA GSOD 2023 sample for 10 stations. Provenance + license per file in `data_src/README.md`, integrity via `SHA256SUMS`. **No network needed, ever** — this kills clean-clone friction for 5 of 6 sources.\n- For each dataset I author one *accurate* data card (realistic prose: overview, schema table, coverage notes, caveats). Then `make_cases.py` applies a **seeded corruption spec** per case: 3–6 discrepancies drawn from 8 operators in `corruptions.py` (inject nulls, add/rename a category, shift a numeric range, drop/duplicate rows vs stated count, stale the date coverage, perturb a claimed mean/percentage, add a phantom column to the card, coarsen \"roughly X%\" language). Each operator edits **either the card or the data**, and emits the gold label + span automatically → `eval/gold/case_XX_gold.json`. Two cases per dataset (different corruption draws) = 12 cases. Every case also keeps 2–3 *true* claims and 1–2 *unverifiable* prose claims so \"holds\" and abstention are genuinely tested.\n- **The hard case (`case_12`):** fully synthetic 1,000,000-row transactions parquet generated deterministically in-code (`numpy` PCG64, seed 20260829 — no download, ~25MB, generated by `make data` in <1 min). Its only violations: a rare status category `REFUND_X` appearing 173 times entirely after row ~810,000, and the card's \"roughly 10% of `coupon_id` values are missing\" when the truth is 14.2%. Head-of-file sampling provably fails the first; threshold judgment on fuzzy language tests the second.\n\n**Scoring script (`eval/score.py`):** loads gold + a system's output JSON → span-align → confusion matrix → macro-F1, per-class P/R, per-case table. `eval/run_all.py` runs baseline, baseline-plus, and agent on all 12 cases and writes `results/results.md`:\n\n```\n| case | system | claims | holds P/R | violated P/R | unverif P/R | macro-F1 | violations caught | cost | time |\n```\n\nplus a full per-claim appendix (`results/per_claim.csv`: case, claim span, gold, baseline verdict, agent verdict, computed value). Complete tables for **both** systems are committed, per the brief.\n\n---\n\n## 6. Planned Improvement Changelog (CHANGELOG.md skeleton)\n\n1. **v0 — Harness + naive agent.** Baselines and a single-session agent that extracts claims and issues verdicts in one conversation with the executor tool. *Failure observed:* verdicts issued for claims whose checks never ran; misses everything past head. *Evidence:* v0 results table (expect macro-F1 ≈ baseline, ~0.45–0.55).\n2. **v1 — Split pipeline + full-data execution.** Dedicated extractor and per-claim synthesizer; every verdict must cite an executor result or abstain. *Fixes:* hallucinated confirmations. *Evidence:* violated-class recall jump, case_12 rare-category catch appears; per-claim diff v0→v1.\n3. **v2 — Mutation-testing the checks (the standout).** Mutant fixtures gate every check. *Fixes:* vacuous-green checks — measure and publish the first-draft vacuous rate (hypothesis ~15–25%). *Evidence:* count of checks rejected by mutants + macro-F1 before/after + two concrete vacuous-check exhibits (e.g., a range check that compared `str` to `int` and always passed).\n4. **v3 — lessons.md memory + abstention calibration.** Recurring synthesis pitfalls appended after each eval iteration and injected into the synthesizer; explicit abstention rules for fuzzy-language claims (\"roughly X%\" → tolerance band). *Fixes:* repeated dtype/NaN bugs and over-confident verdicts on prose. *Evidence:* retries-per-claim trend across cases; unverifiable-class F1 before/after.\n5. **REMOVED — stratified-sample context stuffing.** Experiment: feed the baseline-plus three stratified 2,000-row samples in context instead of executing code (the \"maybe we don't need code\" test). *Result to record:* still misses the row-810k category (sampling probability of the 173/1M slice), 8–10x token cost, no F1 gain over baseline-plus → removed, kept in changelog with its numbers as required. (If the Haiku-synthesizer ablation loses F1, it is written up the same way; if it holds F1, it ships as the cost-optimized default — either outcome is a changelog entry with evidence.)\n\n---\n\n## 7. Tech Stack\n\n- **Python 3.12** (single `pyproject.toml`, `uv` lockfile committed; pip fallback documented).\n- **claude-agent-sdk** (Python, latest — pin exact version in lockfile at build time), **anthropic** SDK for the baseline's single call.\n- **pandas 2.2.x**, **pyarrow ≥ 17** (parquet), **numpy** (seeded case generation), **pydantic 2.x** (schemas), **rapidfuzz** (fallback alignment only), **rich** (live terminal trajectory — this is the video's visual), **pytest** (harness self-tests: corruption operators round-trip, scorer on hand-labeled fixtures).\n- **Models:** `claude-sonnet-4-5` (temperature 0) for extractor/synthesizer/reporter and the baselines; `claude-haiku-4-5` for the synthesizer ablation. Model IDs pinned in one `config.py`.\n- **Sandbox note (Windows-honest):** executor = fresh `subprocess` python, temp cwd, 60s timeout, capped stdout; no `resource` module on Windows, so the cap is timeout + output size, disclosed in README. Nothing needs network at runtime.\n- **Approximate cost:** agent run ≈ 5–8 Sonnet calls/case × ~3k tokens ≈ $0.30–0.60/case → full 12-case eval ≈ **$5–8 per sweep**; ~6 sweeps during development + baselines + ablation ≈ **$50–80 total**. Judge reproduction: one sweep ≈ $6–10, ~25–40 min wall clock. These numbers get measured and written into REPRODUCE.md.\n\n---\n\n## 8. Schedule (H0 = now, Aug 29; deadline H44 = Aug 31 18:00 UTC; ~31 working hours, 2 sleep blocks)\n\n| Hours | Block | Work |\n|---|---|---|\n| H0–H2 | Setup | Repo scaffold, pyproject + lockfile, `schemas.py`, `config.py`; download the 6 raw datasets into `data_src/` + SHA256SUMS + licenses (the only network step, done now, by me, never by judges). |\n| H2–H5 | Cases first | `corruptions.py` (8 operators), `make_cases.py`, author 6 accurate data cards, generate cases 1–6 + gold JSON. Cases before agent = harness drives development. |\n| H5–H7 | Baselines | `run_baseline.py` (+ baseline-plus), output schema, `eval/score.py` with span alignment + pytest fixtures for the scorer. **First real numbers tonight.** |\n| H7–H8 | Buffer / stop point | Sleep block 1 (≈6h, off the clock through H14). |\n| H14–H18 | Agent core (v1) | Extractor + synthesizer agents, profile tool, sandboxed executor, ledger; `run_agent.py` end-to-end on case_01. |\n| H18–H21 | Mutation loop (v2) | `mutants.py` fixtures per claim type, gate + rewrite loop, vacuous-rate counter. |\n| H21–H23 | Full sweep | Cases 7–11 + hard case_12 generator; run v1 and v2 sweeps; commit results; log observed failures into CHANGELOG. |\n| H23–H26 | v3 + ablations | lessons.md loop, abstention calibration; run the stratified-sampling removed-experiment and Haiku ablation; final sweep → `results/`. |\n| H26–H28 | Reporter polish | Make `audit.md` genuinely sign-worthy; run DocDrift on **one real unmodified Kaggle data card** (e.g., the taxi data dictionary) for the video kicker; save that ledger. |\n| H28–H34 | Sleep block 2 + slack | Rest ≈6h; slack absorbs overruns from any block above. |\n| H34–H37 | Writing | README (architecture + results + hot take), REPRODUCE.md **verified from an actual clean clone in a fresh venv on a second Windows user account** (record runtime + cost), DISCLOSURE.md, finalize CHANGELOG, commit trajectories for every agent + baseline + coding-agent excerpts. |\n| H37–H40 | Video | `video/script.md`: (1) 30s problem, (2) live `run_agent.py case_12` with rich trajectory catching the row-810k violation + offending rows, (3) results bar chart baseline vs baseline-plus vs agent, (4) real-Kaggle-card kicker, (5) hot take. Record 2 takes, cut to <5 min. |\n| H40–H43 | Hard buffer | Fix whatever the clean-clone rehearsal or video surfaced; re-run final sweep if any code changed; final commit. |\n| H43–H44 | Submit | Package, links check, submit. Nothing new after H43. |\n\n---\n\n## 9. Deliverables Checklist → Required Items\n\n1. **Code + Improvement Changelog:** full repo; `CHANGELOG.md` with v0→v3 entries + the removed stratified-sampling experiment, each linked to `results/` diffs and run ledgers.\n2. **Reproduction guide:** `REPRODUCE.md` — clean clone → `uv sync` → `make data` (checksums verify; case_12 synthesized) → `python run_baseline.py case_04` → `python run_agent.py case_04` → `python eval/run_all.py` → `python eval/score.py`; pinned versions, measured runtime (~25–40 min sweep) and cost (~$6–10); only external need = an Anthropic API key; verified on a real clean environment at H34.\n3. **Video ≤5 min:** per `video/script.md` above; the two beats are the eval chart and the live hard-case catch, closing on the real-Kaggle unplanted-error kicker.\n4. **Agent trajectories:** `trajectories/` — full ledger + message stream for one normal case and the hard case (covering extractor, synthesizer, mutation loop, reporter), one baseline transcript, plus `trajectories/coding_agent/` disclosing Claude Code build sessions.\n\n---\n\n## 10. Top 3 Risks & Mitigations\n\n1. **Claim-to-gold alignment gets mushy (the judges' explicit hook).** Mitigation: spans, not similarity — gold claims are character spans emitted by the generator; systems must quote their span; primary alignment is deterministic span-IoU, fuzzy matching demoted to a flagged fallback with every fallback match published for inspection. Scorer has pytest fixtures with hand-labeled tricky paraphrases. Decision gate at H7: if span-quoting compliance <95% in early runs, add a one-line output-format retry (cheap) before ever touching the card format.\n2. **Clean-clone friction (downloads + 1M-row file).** Mitigation: all six real datasets are small samples committed in-repo with licenses and checksums; the 1M-row hard case is *generated* deterministically by `make data` in under a minute; judge path requires zero network beyond the Anthropic API. Rehearsed from an actual fresh clone + venv at H34, with both PowerShell and bash commands documented.\n3. **Agent loop burns the clock (SDK friction, flaky JSON, runaway retries).** Mitigation: orchestrator is deterministic Python — LLM calls are single-purpose with pydantic-validated outputs and a hard cap of 2 retries per step; if the Agent SDK client misbehaves on Windows, the identical pipeline runs on plain `anthropic` tool-use calls (stages are already isolated functions, swap is ~1 hour). Scope fuse: if H21 arrives without v2 green, cut cases 10–11 (10 cases still meets the bar) rather than cutting the mutation loop — the mutation loop is the submission.\n\n---\n\n## 11. Candidate Hot Takes (validate while working)\n\n1. **\"LLM-written verifiers fail exactly like human-written ones: green does not mean tested. N% of first-draft checks passed vacuously until we mutation-tested the checks themselves.\"** Validation is built in: the v2 gate logs every check rejected by a mutant fixture; publish the rate, two concrete vacuous-check exhibits (e.g., a dtype-coerced comparison that could never fail), and the v1→v2 F1 delta.\n2. **\"More context made the baseline more confident, not more correct.\"** Validation: compare baseline vs baseline-plus per-claim — hypothesis is that `describe()` output *increases* confident 'holds' verdicts on claims describe() cannot actually settle (rare categories, row-position effects, cross-column rules), raising false-alarm-free confidence while violated-recall barely moves. If the data shows it, one scatter of confidence-vs-correctness per baseline tells the story; if it doesn't, fall back to the abstention lesson (the agent initially \"verified\" provenance prose it had no way to check — calibrated abstention was worth more F1 than any smarter check)."}
