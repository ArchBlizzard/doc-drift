case_id: case_11
dataset: taxi_jan2023_50k
seed: 211
corruptions:
  - op: add_category
    target: data
    claim: t_flag
    params: { new_value: U, n: 6 }
  # exact-value claims are falsifiable in either direction (real min is -351.00)
  - op: perturb_stat
    target: card
    claim: t_min_total
    params: { new_span: "Refunded trips push the minimum total to -$99.00." }
  - op: perturb_stat
    target: card
    claim: t_rows
    params: { new_span: "This sample holds 52,500 trips" }
kept_true_claims: 4
kept_unverifiable_claims: 1
