case_id: case_05
dataset: taxi_jan2023_50k
seed: 105
corruptions:
  - op: shift_range
    target: data
    claim: t_pass
    params: { out_value: 9.0, n: 3 }
  - op: perturb_stat
    target: card
    claim: t_fare
    params: { new_span: "The average total fare is $34.05." }
  - op: phantom_column
    target: card
    params:
      claim_id: t_phantom
      column: driver_id
      sentence: "A `driver_id` column identifies the operator on every trip."
      anchor: "The `store_and_fwd_flag` column is either `N` or `Y`."
  - op: stale_temporal
    target: card
    claim: t_dates
    params: { new_span: "Pickup timestamps run from 1 January 2023 to 31 January 2023" }
kept_true_claims: 4
kept_unverifiable_claims: 1
